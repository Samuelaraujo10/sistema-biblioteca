NODE_ENV=development

# Porta do servidor
PORT=3000

# Chave secreta para sessions (gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
# Em produção, gerar uma chave nova segura
SESSION_SECRET=biblioteca-escolar-secret-key-2026
